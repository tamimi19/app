rootProject.name = "OneUIExample"
include ':app'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // هذا هو المستودع الضروري
        maven { url 'https://jitpack.io' }
    }
}
